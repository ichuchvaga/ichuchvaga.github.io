//- Все примеси в этом файле должны начинаться c имени блока (blog-card)

mixin blog-card(text, mods)

  //- Принимает:
  //-   text    {string} - текст
  //-   mods    {string} - список модификаторов
  //- Вызов:
        +blog-card('Текст', 'some-mod')

  -
    // список модификаторов
    var allMods = '';
    if(typeof(mods) !== 'undefined' && mods) {
      var modsList = mods.split(',');
      for (var i = 0; i < modsList.length; i++) {
        allMods = allMods + ' blog-card--' + modsList[i].trim();
      }
    }

  a.blog-card(href="blog-single.html", class=allMods)&attributes(attributes)
    img(src="img/blog-card-image.png", alt="все о бриллиантах").blog-card__image
    .blog-card__title Все о бриллиантах
    .blog-card__text Украшения Diamondlake уникальны и неповторимы. Все кольца создаются вручную с вниманием к каждой детали.

