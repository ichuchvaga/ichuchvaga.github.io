extends ../pug/layout-ru.pug

block variables
    - var pageClass = 'page--catalog';

block meta
    title Main
    meta(name='description', content='')

block content
    +catalog-banner('img/catalog-banner-moms-left.png', 'img/catalog-banner-moms-right.png', 'Mom’s Bracelets', 'Designed with inspiration and crafted with love')
    .container
        +breadcrumbs()
            +breadcrumbs-item('Home', '#')
            +breadcrumbs-item('Mom’s Bracelets')

    +catalog('moms')