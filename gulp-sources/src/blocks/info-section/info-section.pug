//- Все примеси в этом файле должны начинаться c имени блока (info-section)

mixin info-section(mods)

  //- Принимает:
  //-   text    {string} - текст
  //-   mods    {string} - список модификаторов
  //- Вызов:
        +info-section('Текст', 'some-mod')

  -
    // список модификаторов
    var allMods = '';
    if(typeof(mods) !== 'undefined' && mods) {
      var modsList = mods.split(',');
      for (var i = 0; i < modsList.length; i++) {
        allMods = allMods + ' info-section--' + modsList[i].trim();
      }
    }

  .info-section.section(class=allMods)&attributes(attributes)
    .container
      block