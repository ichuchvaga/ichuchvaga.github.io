/* global document */

import ready from 'Utils/documentReady.js';

ready(function() {
  /*
  document.addEventListener('click', (event) => {
    const category_card = event.target.closest('.category-card');
    if ((event.target.className == 'category-card__links') || (event.target.className == 'category-card__link') || (event.target.className == 'cat-circle-link')) {
      event.stopPropagation();
    } else {
      
      if (category_card) {
        event.preventDefault();
        window.location.href = category_card.dataset.href;
      }      
    }
  });*/
});
